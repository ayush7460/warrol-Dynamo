export const sendBookingConfirmation = async (booking, ticketBuffer) => {
  // TODO: Integrate with nodemailer or any email service provider
  // Send email with ticketBuffer as attachment (PDF)
  return true;
};